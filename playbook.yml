---
- name: Common ansible playbook
  hosts: all
  roles:
    - minimal
    - { role: dotdeb, when: use_dotdeb }
    - { role: mariadb, when: use_database == 'mariadb' }
    - { role: php, when: use_php }
